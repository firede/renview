# 弃案与撤回记录

被否决或已撤回的方案及理由。**不需要每次通读**；提出新方向/新能力之前先查这里，避免重提弃案。
当前产品取舍见 `.agents/truth/product.md`；删除的实现记录由 git 历史保留，无需搬运到这里。

## product

- 实现摘要注释行（P2，落地于 23c9fe9，撤回于 2b39706）：body 变更单元触及 ≥2 条顶层语句时，在行流中插注释色摘要行（语句首行截断的枚举）。撤回理由：内容是"被触及语句的位置枚举"，与词级高亮后的行流信息重复；首行截断产生半截语法，人类解读成本高于直接读代码。若复活须先解决 delta 语义（条件新旧对照等完整语义单元，而非语句位置枚举）；代码 `insertBodyNotes` 与测试保留未接线。

## architecture

- 实现摘要数据流（随上条撤回而休眠）：bodySummary 条目带新侧行号，insertBodyNotes 以简化行替换原始预览、按 `newLn ?? oldLn` 启发式插入 SRow note 行；server 已不接线，复活时从同一入口接入。
