# renview 开发指南

帮助人类降低认知负担的代码审核工具。用法与开发命令见 [README.md](README.md)。

## 决策记录制度（最高优先级约定）

维护者可逐行审核的代码量有限，项目方向通过 `.agents/truth/` 的决策记录来掌控。**生效的决策必须在做出时同步记录**，否则视为工作未完成。

- 记录位置：`.agents/truth/` 下按类别分文件，现有 `product.md`（定位、交互、用户可见行为、范围红线）、`architecture.md`（数据流、模块边界、跨模块不变量、降级策略、已知局限）、`stack.md`（依赖与版本锁定理由、构建分发）。现有类别放不下时可新增类别文件。truth/ 只装现状；被否决或撤回的方案入 `.agents/archived.md`。
- 收录标准（反事实检验）：**这条若被删掉，未来的维护者或 agent 会无意违背它吗？** 会才收录。据此收录：生效的取舍、范围红线、跨模块不变量、依赖与锁版的理由、降级策略、已知局限。
- 不收录：调研过程与一次性结论（兼容性实测、竞品核查、包名查证等，要留痕就并入相关决策的理由从句，不设调研日志）；未生效的方案否决（会被反复重提的替代路线入 `.agents/archived.md`）；代码或 lockfile 可读的实现细节（版本号、像素值、节点类型清单、CSS 覆盖方式）；临时 TODO 与未定设想（"拟做"在落地时才成为决策）。
- 弃案归档：被否决/撤回的条目从 truth/ 移入 `.agents/archived.md`（结论 + 理由 + 出处 commit，只增不删）；防重提的机制是提案时查阅，而非让每次通读都背弃案。
- 格式：一条一句话，先结论后理由，写到能说清"为什么"为止，不展开论述。只写"是什么"不写"为什么"的条目视为不合格，补理由或删除。
- 决策被推翻时改写原条目，不堆积过期结论（历史由 git 与 archived.md 留存）。
- 动手前先通读 `.agents/truth/`，不得无意违背已有决策；确需推翻时在提案中明说。提出新方向/新能力前另查 `.agents/archived.md`，避免重提弃案。

## 代码约定

- 注释、文档、提交信息一律中文；提交信息用 `feat|fix|test|docs|chore|refactor: 摘要` 格式。
- 测试用 `bun:test`，放 `test/*.test.ts`；改分析层逻辑必须同步补测试。
- 提交前跑 `bun run test` 与 `bun run typecheck`，保持全绿；pre-push 钩子（simple-git-hooks，`bun install` 时经 prepare 自动安装）强制同一道检查，紧急时用 `git push --no-verify` 跳过。
- 注意 `bun test`（裸命令）与 `bun run test`（含 sync:wasm）的区别：CI、钩子等干净环境一律用 `bun run test`。
