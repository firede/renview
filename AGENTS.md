# renview 开发指南

帮助人类降低认知负担的代码审核工具。用法与开发命令见 [README.md](README.md)。

## 决策记录制度（最高优先级约定）

维护者可逐行审核的代码量有限，项目方向通过 `.agents/truth/` 的决策记录来掌控。**调研、讨论、实践中得出的重要决策，必须在做出时同步记录**，否则视为工作未完成。

- 记录位置：`.agents/truth/` 下按类别分文件，现有 `product.md`（定位、交互、用户可见行为、取舍与明确不做的事）、`architecture.md`（数据流、模块边界、算法、降级策略、已知局限）、`stack.md`（依赖与版本锁定理由、构建分发）。现有类别放不下时可新增类别文件。
- 格式：一条一句话，先结论后理由，写到能说清"为什么"为止，不展开论述。
- 记录时机：做出取舍（含被否决的方案）、接受已知局限、增删依赖、改变用户可见行为、调整模块边界。
- 不记录：能从代码直接读出的实现细节、临时 TODO、未定的设想。
- 决策被推翻时改写原条目，不堆积过期结论（历史由 git 留存）。
- 动手前先通读 `.agents/truth/`，不得无意违背已有决策；确需推翻时在提案中明说。

## 代码约定

- 注释、文档、提交信息一律中文；提交信息用 `feat|fix|test|docs|chore|refactor: 摘要` 格式。
- 测试用 `bun:test`，放 `test/*.test.ts`；改分析层逻辑必须同步补测试。
- 提交前跑 `bun test` 与 `bun run typecheck`，保持全绿。
