{
  "name": "renview-www",
  "type": "module",
  "version": "0.1.0",
  "homepage": "https://view.bandwidth.ren",
  "private": true,
  "scripts": {
    "dev": "bun run prepare:viewer && astro dev",
    "build": "bun run prepare:viewer && astro build",
    "preview": "wrangler dev",
    "prepare:viewer": "bun install --cwd .. --frozen-lockfile --ignore-scripts"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^14.2.6",
    "astro": "^7.2.10"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "wrangler": "^4.128.0"
  }
}
